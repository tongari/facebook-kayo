<div class="c-group -space-XS u-space-t-XL">
  <%- if controller_name != 'sessions' %>
      <%= link_to 'ログイン', new_session_path(resource_name), class: 'u-block' %>
  <% end -%>

  <%- if devise_mapping.registerable? && controller_name != 'registrations' %>
      <%= link_to 'アカウント登録', new_registration_path(resource_name), class: 'u-block' %>
  <% end -%>

  <%- if devise_mapping.recoverable? && controller_name != 'passwords' && controller_name != 'registrations' %>
      <%= link_to 'パスワードをお忘れの方', new_password_path(resource_name), class: 'u-block' %>
  <% end -%>

  <%- if devise_mapping.confirmable? && controller_name != 'confirmations' %>
      <%= link_to '確認メールが届かない方', new_confirmation_path(resource_name), class: 'u-block' %>
  <% end -%>

  <%- if devise_mapping.lockable? && resource_class.unlock_strategy_enabled?(:email) && controller_name != 'unlocks' %>
      <%= link_to 'ロック解除メールが受信できない方', new_unlock_path(resource_name), class: 'u-block' %>
  <% end -%>

  <%- if devise_mapping.omniauthable? %>
      <%- resource_class.omniauth_providers.each do |provider| %>
          <%= link_to "#{OmniAuth::Utils.camelize(provider)}でログイン", omniauth_authorize_path(resource_name, provider), class: 'u-block' %>
      <% end -%>
  <% end -%>
</div>

